// Top-level build file

buildscript {
    dependencies {
        classpath("com.android.tools.build:gradle:8.2.0") // or latest stable
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.0") // or match your Kotlin version

    }
    repositories {
        google()
        mavenCentral()
    }
}
